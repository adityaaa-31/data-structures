#include <stdio.h>
#include <stdlib.h>
#define MAX 5

struct queue{

    int data[MAX], front rear;
};

void initq(struct queue *q)
{
    q -> front = q -> rear = -1;
}

int isEmpty(struct queue *q)

int main()
{

    return 0;
}
